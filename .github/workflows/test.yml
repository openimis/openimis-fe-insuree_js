name: Run Module Tests

on:
  push:
    branches: [develop, feature*, release*]
  pull_request:
    branches: [develop, feature*, release*]

jobs:
  run-tests:
    uses: Y-Note-SAS/openimis-fe_js/.github/workflows/test-module.yml@feature-34644
    with:
      module-path: .
